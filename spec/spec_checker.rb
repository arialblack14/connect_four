require_relative '../lib/checker.rb'
require_relative '../spec/spec_helper'
require 'rspec'

describe Checker do
  let(:checker) { Checker.new }
  it "when one's player's turn drop one checker down the grid" do
    checker
  end
end